# 开发日志 — 2026-06-03

## 完成事项

### 阶段二核心功能实现

- [x] **Rating 系数表修正** — `constants.ts` COEFFICIENT_TABLE 对齐参考实现 `ScoreCoefficient.js`
  - 旧表系数错位严重（如 97% 显示 20.3 实为 20.0，100% 显示 22.2 实为 21.6）
  - 补齐缺失的边界阈值（96.9999/79.9999/0.0 等）
  - 移除未使用的 `max` 字段，新增 `rate` 标签

- [x] **Rating & B50 算法工具** — `src/utils/rating.ts`
  - `computeRating(levelValue, achievements)` — 单曲 DX Rating
  - `computeB50(scores, songMap)` — B50 完整算法
    - 按 (songId + levelIndex) 去重取最高分
    - 按 Rating → 定数 → 达成率 降序排列
    - isNew 判定新旧版本，分为 best35 / best15 两池
    - 宴会场 (songId ≥ 100000) 自动排除

- [x] **曲目数据 IndexedDB 缓存** — `database.ts` v2 迁移
  - 新增 `songCache` 表（24h TTL）
  - `getCachedSongs()` / `setCachedSongs()` 辅助函数
  - songStore.fetchSongs() 缓存优先加载 + 后台静默刷新

- [x] **Diving-Fish 玩家查分** — `divingFishApi.ts` + `adapter.ts`
  - `fetchPlayerScores(username)` — POST 查询 B50
  - `toInternalDfScore()` — DfChartInfo 格式转换（DfScore 统一格式）

- [x] **playerStore** — 新 Zustand store
  - 在线查分（Diving-Fish）：dfBest35/dfBest15/dfRating
  - 本地 B50（基于 IndexedDB + songStore）：computeLocalB50()

- [x] **PlayerInfo 页面** — 完整实现
  - Tab 切换：Diving-Fish 在线查分 / 本地 B50
  - 在线模式：输入用户名 → 查询 → B50 列表 + 总 Rating
  - 本地模式：基于本地录入成绩自动计算 B50
  - 加载/错误/空结果状态处理

- [x] **五维分析雏形** — `utils/dimensions.ts` + `DimensionAnalysis.tsx`
  - `computeChartDimensions(chart, bpm)` — 单谱面五维评分
  - `computePlayerDimensions(scores, chartDimensions)` — 平方加权平均
  - CSS 简易条形图（底力/体力/爆发/定位/技巧）
  - 维度说明 + 代表谱面

- [x] **SongDetail.tsx** — 更新为使用共享 `computeRating`

## 构建结果
```
dist/assets/PlayerInfo-OLev67vn.js          12.02 kB
dist/assets/DimensionAnalysis-CHMi9aO0.js    5.58 kB
dist/assets/rating-Mkbp43di.js               1.10 kB
dist/assets/scoreStore-gvgcyhdV.js           0.67 kB
dist/assets/SongList-Bx5RRu4G.js             7.08 kB
dist/assets/SongDetail-DTf88SUX.js          13.99 kB
dist/assets/songStore-CRIWo3WB.js          102.35 kB  (含歌曲 JSON 数据)
dist/assets/index-DDvf6vLI.js              238.62 kB
```
TypeScript 零错误，Vite 构建通过。

- [x] **浅色主题美化** — 暗色 PRiSM 主题 → 白色+浅蓝为主的浅色主题
  - `src/index.css` — 全部 34 个 CSS 变量替换（背景/品牌/文字/边框/语义/难度/评级）+ 渐变改为蓝→紫
  - `src/data/constants.ts` — LEVEL_INDEX_MAP + RATE_COLORS 对齐设计规范
  - `src/pages/DimensionAnalysis.tsx` — 五维颜色更新
  - `src/pages/SongDetail.tsx` — 非选中难度标签改为透明底+彩色边框
  - `src/pages/SongList.tsx` — 封面占位 SVG 颜色
  - `src/components/layout/Sidebar.tsx` — 移动端遮罩减淡 + 活跃态指示器改用紫色
  - `src/components/ScoreForm.tsx` — 模态遮罩减淡
  - `index.html` — 新增 M PLUS Rounded 1c 字体
  - TypeScript 零错误，Vite 构建通过

- [x] **分数录入 dxRating 修复** — `ScoreForm.tsx` 提交时 dxRating 硬编码为 0
  - 导入 `computeRating`，提交时动态计算 `computeRating(levelValue, achievements)`

- [x] **白屏修复** — Google Fonts 样式表阻塞首屏渲染
  - 诊断：`<link rel="stylesheet">` 加载 Google Fonts 是渲染阻塞的，网络不通时等待 ~10s 超时
  - 修复：`<link rel="preload" as="style">` + `onload` 非阻塞模式 + `<noscript>` 回退
  - 效果：渲染时间 22s → 1s

- [x] **Diving-Fish 一键导入成绩** — Step 17 实现
  - `adapter.ts` — 新增 `toInternalScore(DfScore) → Score` 转换（title→songTitle, 补 playDate）
  - `database.ts` — 新增 `bulkUpsertScores()` 按 [songId+levelIndex] 去重保留最高分，单事务原子写入
  - `scoreStore.ts` — 新增 `bulkImportScores()` 批量写入 + 全量重读刷新本地数组
  - `playerStore.ts` — 新增 `importDivingFishScores()` + `recomputeFlag`（修复更新不触发 B50 重算的 Bug）
  - `PlayerInfo.tsx` — 新增「从查分器导入成绩」按钮、导入进度卡片（加载/成功/错误三态）、placeholder 改为 "maimai"
  - 去重策略：同 [songId+levelIndex] 保留更高达成率，本地高分不被覆盖

- [ ] **测试 Diving-Fish 查分 API 实际可用性** — 测试完成
  - `POST /api/maimaidxprober/query/player` — HTTP 200, ~0.77s, ~5KB ✅
  - `GET /api/maimaidxprober/music_data` — HTTP 200, ~0.34s, ~872KB ✅
  - Vite 代理正常转发 ✅
  - 字段映射（DfChartInfo→DfScore→Score）全部对齐 ✅
  - 边界测试：不存在的玩家返回 `{"message":"user not exists"}`（HTTP 200）
  - **修复**：不存在玩家错误处理 — API 返回 HTTP 200 + error JSON，`fetchPlayerScores` 新增 `data.charts` 检查，抛出清晰错误消息避免 `TypeError`

- [x] **Diving-Fish API 文档调研** — 阅读官方 API 文档（SingleFile 保存 HTML），关键发现：
  - Diving-Fish 使用 `username`/`qq` 作为玩家标识，**无 `friend_code` 概念**
  - 全量成绩接口：`GET /player/records`（Import-Token）或 `GET /dev/player/records`（Developer-Token）
  - Import-Token 由用户自行在 Diving-Fish 个人资料页生成，适合个人工具场景
  - **Bug 发现**：当前 `fetchPlayerScores` 未传 `b50: true`，导致获取的是 B40 而非 B50
  - 不存在玩家返回 HTTP 200 + `{"message":"user not exists"}`，已修复

## 待办
- [x] 修复 Step 14：`fetchPlayerScores` 请求体增加 `b50: true`（当前只获取 B40）
- [x] 实现 Step 17：全量成绩一键导入（基于 Import-Token）
  - 新增 `GET /player/records` 接口调用
  - UI 增加 Import-Token 输入框 + localStorage 持久化
  - 支持用户名+密码登录自动获取 Import-Token（`/login` + `PUT /player/import_token`）
  - 全量成绩批量写入 IndexedDB
- [ ] 阶段三：推分建议（基于 B50 分析推荐可提升 Rating 的谱面）
- [ ] 阶段四：ECharts 雷达图 + 维度代表谱面 + 高分推荐/低分练习曲

## 修改文件清单
| 文件 | 改动 |
|------|------|
| `src/data/constants.ts` | COEFFICIENT_TABLE 修正（对齐 ScoreCoefficient.js）+ 移除 max 字段 |
| `src/utils/rating.ts` | 重写 → computeRating + computeB50 算法实现 |
| `src/utils/dimensions.ts` | 新建 → 五维分析工具函数 |
| `src/db/database.ts` | v2 迁移 → songCache 表 + 缓存 CRUD |
| `src/services/divingFishApi.ts` | fetchPlayerScores 实现（POST 查分） |
| `src/services/adapter.ts` | 新增 toInternalDfScore + DfScore 类型 |
| `src/store/songStore.ts` | fetchSongs 缓存优先加载 + 后台刷新 |
| `src/store/playerStore.ts` | 新建 → 在线查分 + 本地 B50 计算 |
| `src/pages/PlayerInfo.tsx` | 重写 → 在线查分/本地 B50 Tab + 完整 UI |
| `src/pages/DimensionAnalysis.tsx` | 重写 → 五维条形图 + 维度说明 |
| `src/pages/SongDetail.tsx` | 改用共享 computeRating |
| `src/components/ScoreForm.tsx` | dxRating 修复 + 模态遮罩减淡 |
| `index.html` | 字体加载 → 非阻塞模式（修复白屏） |
| `docs/04-执行步骤.md` | 新增 Step 17（Diving-Fish 一键导入） |
| `docs/01-需求规格.md` | 新增「玩家成绩导入方式」段落 |
| `src/services/adapter.ts` | 新增 toInternalScore 转换 |
| `src/db/database.ts` | 新增 bulkUpsertScores（事务内去重 upsert） |
| `src/store/scoreStore.ts` | 新增 bulkImportScores action |
| `src/store/playerStore.ts` | 新增 importDivingFishScores + recomputeFlag |
| `src/pages/PlayerInfo.tsx` | 导入按钮 + 进度卡片 + placeholder 改为 "maimai" + useEffect 修复 |
| `docs/04-执行步骤.md` | 阶段一全部勾选 + 阶段二步骤补充 |
| `src/services/divingFishApi.ts` | 修复不存在玩家错误处理（API HTTP 200 + error JSON 检查） |
| `src/db/database.ts` | 新增 `clearAllScores()` |
| `src/store/scoreStore.ts` | 新增 `clearAllScores` action |
| `src/pages/PlayerInfo.tsx` | 本地 B50 标签页新增「清除本地成绩数据」按钮 + 二次确认 |
| `docs/01-需求规格.md` | 重写「玩家成绩导入方式」：基于 Import-Token 的全量成绩导入方案 |
| `docs/02-技术选型.md` | 完整重写 API 依赖章节：Diving-Fish 全量接口 + 认证方式 + LXNS 未来兼容 |
| `docs/04-执行步骤.md` | Step 14 补充 B40→B50 修复项；Step 17 重写为全量成绩导入 + Import-Token 方案 |
| `docs/04-执行步骤.md` | Step 17/18 全部勾选完成 + Vite cookieDomainRewrite 说明 |
| `src/services/divingFishApi.ts` | Step 18：请求体加 `b50: true` 修复 B40 Bug；新增 `loginToProber`、`generateImportToken`、`fetchPlayerRecords` |
| `src/store/playerStore.ts` | `importDivingFishScores` 重写为 Import-Token + 全量接口（GET /player/records） |
| `src/pages/PlayerInfo.tsx` | 新增 Import-Token 输入框 + localStorage 持久化 + LoginToGetToken 内联登录组件 |
| `vite.config.ts` | 代理增加 `cookieDomainRewrite: 'localhost'`（支持登录 Cookie 转发） |
| `开发日志/2026-06-03.md` | 更新待办与修改文件清单 |

---

## v0.3.0 结构性调整

### 完成事项

- [x] **搜索功能优化** — 多关键词 AND 搜索 + 别名 + 定数预设 + 版本分国服 + 难度筛选
  - `src/data/versions.ts` — VERSION_REGIONS 国服/日服版本分组常量
  - `src/data/aliases.ts` — 社区别名 API（Yuri-YuzuChaN/maimaiDX）→ 反向索引 → 子串匹配
  - `src/db/database.ts` — v3 迁移新增 aliasCache 表（24h TTL）
  - `vite.config.ts` — 新增 /alias-api 代理到 yuzuchan.moe
  - `src/store/songStore.ts` — AdvancedFilters 移除 artist，新增 region/difficultyLevel；getFilteredSongs 重写为分词 AND 搜索（title|artist|noteDesigner|aliases）

- [x] **侧边栏用户卡片** — `src/components/layout/UserInfoCard.tsx`
  - 读取 playerStore（localB50 优先 → dfRating → 未登录）
  - 折叠态显示 Rating 数值，展开态显示名字 + Rating
  - 未登录点击跳转 /songs

- [x] **分页组件** — `src/components/shared/Pagination.tsx`
  - 50条/页，0-based
  - ≤7 页全显示，>7 页省略号
  - 翻页自动滚回顶部

- [x] **SongList.tsx 重写**
  - 高级筛选面板简化：移除 artist 输入框，新增定数预设按钮（13/13+/14/14+/15）、区域（国服）、难度（BASIC~Re:MASTER）
  - 分页集成
  - 导入成绩按钮 + 内联 Import Modal（从 PlayerInfo 迁移）

- [x] **PlayerInfo.tsx 手术式删减**
  - 移除 Import-Token 区块 + LoginToGetToken 组件
  - 保留双 Tab 结构（Diving-Fish 查分 / 本地 B50）
  - 空状态添加「前往曲目查询页」链接

- [x] **Sidebar/MobileBottomNav 更新**
  - Sidebar: 插入 UserInfoCard, 导航标签 '玩家信息'→'B50 一览', 版本 v0.2.0→v0.3.0
  - MobileBottomNav: 标签 '玩家'→'B50'

### 构建结果
TypeScript 零错误，Vite 构建通过。
```
dist/assets/index-BvvfXZTo.css              30.41 kB
dist/assets/DimensionAnalysis-CZFtyDws.js    5.52 kB
dist/assets/PlayerInfo-Bqc2Njqc.js          10.59 kB
dist/assets/SongDetail-CHVW0DxG.js          13.92 kB
dist/assets/SongList-DOrZRAxg.js            16.55 kB
dist/assets/jsx-runtime-BWeYMJwa.js        112.97 kB
dist/assets/index-DM-MD9ed.js              234.07 kB
```

### 修改文件清单
| 文件 | 操作 |
|------|------|
| `src/data/versions.ts` | 新建 |
| `src/data/aliases.ts` | 新建 |
| `src/store/songStore.ts` | 重写 |
| `src/db/database.ts` | v3 迁移 |
| `vite.config.ts` | 新增 alias 代理 |
| `src/components/shared/Pagination.tsx` | 新建 |
| `src/components/layout/UserInfoCard.tsx` | 新建 |
| `src/pages/SongList.tsx` | 重写 |
| `src/pages/PlayerInfo.tsx` | 重写 |
| `src/components/layout/Sidebar.tsx` | 修改 |
| `src/components/layout/MobileBottomNav.tsx` | 修改 |

---

## DX 星数评分展示

### 完成事项

- [x] **DX 星数工具函数** — `src/utils/dxStar.ts`（新建）
  - `computeDxStar(dxScore, totalNotes)` → `{ ratio, stars, maxDxScore }`
  - `renderStars(stars)` → Unicode ⭐ 字符串
  - 阈值：97%/95%/93%/90%/85% → 5/4/3/2/1 星

- [x] **SongDetail 成绩行展示** — DX 分数改为 `DX {score}/{max} ({ratio}%)`
  - ScoreRow 新增 `allDiffs` prop → 查找对应难度物量
  - 无物量数据时 fallback 到旧格式

- [x] **PlayerInfo B50 行展示** — 增加 DX 星数和 FS 标记
  - B50Row 新增 `dxScore`/`totalNotes` props → `⭐ {ratio}%` 显示
  - B50Entry 类型新增 `dxScore` 字段
  - 在线 Tab 和本地 Tab 均传入 DX 数据

- [x] **文档更新**
  - `docs/05-算法详解.md` — 新增「6. DX 星数评分」章节
  - `docs/04-执行步骤.md` — 新增 Step 21 + 日后计划
  - `CLAUDE.md` — 核心算法 + 未来计划补充

### 构建结果
TypeScript 零错误，Vite 构建通过。
```
dist/assets/dxStar-CuR5LpkX.js               0.34 kB
dist/assets/PlayerInfo-Bxd3xE9o.js          11.28 kB
dist/assets/SongDetail-CifttjQg.js          14.43 kB
```

### 修改文件清单
| 文件 | 操作 |
|------|------|
| `src/utils/dxStar.ts` | **新建** |
| `src/utils/rating.ts` | 修改 — B50Entry 新增 dxScore 字段 |
| `src/pages/SongDetail.tsx` | 修改 — 成绩行展示 DX 星数 |
| `src/pages/PlayerInfo.tsx` | 修改 — B50 行展示 DX 星数 |
| `docs/05-算法详解.md` | 修改 — 新增「6. DX 星数评分」章节 |
| `docs/04-执行步骤.md` | 修改 — 新增 Step 21 + 日后计划 |
| `CLAUDE.md` | 修改 — 核心算法 + 未来计划补充 |

---

## 6 项 Fix 浏览器验证 + 别名 API Bug 修复

### 发现 Bug
- **别名 API 响应格式不匹配**: `loadAliasData()` 期望 `AliasEntry[]` 数组，但 yuzuchan.moe API 实际返回 `{code: 0, content: AliasEntry[]}` 对象。`Array.isArray()` 检查失败导致别名索引永远为 null，搜索"潘"/"茄"等别名无效。

### 修复
- **`src/data/aliases.ts`** — 修改 API 响应解析：`const raw = await res.json(); const data = Array.isArray(raw) ? raw : raw.content`

### 浏览器自动化验证结果（agent-browser + Edge）

| Fix | 验证项 | 结果 |
|-----|--------|------|
| Fix 1 | DX 分数率 4 位小数 | ✅ `90.4977%` |
| Fix 2 | ScoreHeader 表头 | ✅ 难度/达成率/评级/FC/FS/DX分数/日期/操作 完全对齐 |
| Fix 3 | DX 星数图标 | ✅ `⭐⭐ DX 3000/3315 (90.4977%)` |
| Fix 4 | dxMax 自动填充 | ✅ Master 难度自动填 3315（1105×3），折叠区自动展开 |
| Fix 5a | 别名初始化 | ✅ `loadAliasData()` 在 App.tsx useEffect 调用 |
| Fix 5b | 搜索"潘"→Pandora paradoxxx | ✅ 1 条结果 |
| Fix 5b | 搜索"茄"→qzkago requiem | ✅ 4 条结果含 QZKago Requiem |
| Fix 5c | SongDetail 别名展示 | ✅ 10 个别名标签换行排列 |
| Fix 6 | B50 列对齐 | ✅ FC/FS 空占位 + DX 星数 4 位小数 + Rating 列位置稳定 |

### 修改文件清单
| 文件 | 操作 |
|------|------|
| `src/data/aliases.ts` | 修复 — API 响应格式 `{code, content}` → 提取 `raw.content` |
| `开发日志/2026-06-03.md` | 更新 — 验证记录 |

---

## 阶段三+四：推分建议 + 五维雷达图 (v0.4.0)

### 完成事项

- [x] **Step 3-1: 推分算法 `computePushSuggestions()`** — 基于 B50 地板替换的智能推分推荐
  - `src/utils/rating.ts` — 新增 `PushSuggestion` 类型 + `computePushSuggestions()` 函数
  - `src/utils/rating.test.ts` — 17 个单元测试（computeRating/computeB50/computePushSuggestions）
  - 算法：潜力评分模式 — 计算 `computeRating(levelValue, 100.5)` 作为目标 Rating，判断是否能替换 B35/B15 地板
  - 支持：在 B50 外的候选曲、B50 地板曲的自我提升建议
  - 排除：宴会场 (songId ≥ 100000)、已在 B50 中但非地板的曲目

- [x] **Step 3-2: 推分建议 UI** — `src/components/shared/PushSuggestions.tsx`
  - 表格展示：序号/曲目/定数/当前达成率/目标Rating/增益/难度/替换池
  - 默认显示前 5 条，可展开全部
  - 难度标记：轻松(绿)/适中(橙)/挑战(红)
  - 集成到 PlayerInfo 本地 B50 Tab 下方

- [x] **Step 3-3: 定数阶梯推分策略** — `src/utils/strategy.ts`
  - 6 个定数阶梯：入门(紫框)/进阶(铜~银)/高阶(金)/专家(白金)/大师(万六)/宗师(万六五+)
  - 每个阶梯提供：总评、具体行动项、预估周期
  - 集成到 PlayerInfo — StrategyPanel 组件

- [x] **Step 4-1: ECharts 雷达图** — `src/components/charts/RadarChart.tsx`
  - 树摇优化（仅注册 RadarChart + Tooltip + CanvasRenderer）
  - 五维标签 + 双圈刻度 + 品牌色区
  - DimensionAnalysis 重写为 ECharts 雷达图 + 分数摘要网格

- [x] **Step 4-2: 谱面特征标签 + 短板诊断**
  - `src/utils/chartTags.ts` — 7 种标签（交互/纵连/星星/跳拍/体力/技巧/综合）
  - `src/utils/weaknessAnalysis.ts` — 按标签分组统计达成率，识别主要/次要短板
  - DimensionAnalysis — 短板标签按钮 + 点击展开练习推荐

- [x] **Step 4-3: 练习曲推荐** — `src/utils/practiceRecommend.ts`
  - 按短板标签筛选曲库 → 定数递进排序 → 每 0.3 定数跳跃选最多 2 首
  - 标注已达标曲目作为巩固参考

- [x] **Step 4-4: 推分路线规划** — `src/utils/routePlanner.ts`
  - 4 个阶梯阶段：低定数区/中定数区/高定数区/超高定数区
  - 每阶段 Top 5 候选曲 + 预估 Rating 增益
  - 目标：currentRating → targetRating

- [x] **vitest 测试框架搭建** — `npm install -D vitest` + vite.config.ts test 配置 + package.json test 脚本

- [x] **agent-browser 浏览器验证** — 所有页面渲染正常、导航正常、空状态正确

### 构建结果

TypeScript 零错误，Vite 331ms 通过。
```
dist/assets/PlayerInfo-C2rLkiJM.js          21.24 kB (+9.56 kB，推分建议 + 策略面板)
dist/assets/DimensionAnalysis-CTayHLdL.js  522.74 kB (+517 kB，含 ECharts 库)
dist/assets/index-Dg46u6xN.js              350.89 kB (+115 kB，ECharts 共享代码)
```

### 修改文件清单

| 文件 | 操作 |
|------|------|
| `src/utils/rating.ts` | 修改 — 新增 `PushSuggestion` + `computePushSuggestions()` + `classifyDifficulty` |
| `src/utils/rating.test.ts` | **新建** — 17 个单元测试 |
| `src/components/shared/PushSuggestions.tsx` | **新建** — 推分建议表格组件 |
| `src/utils/strategy.ts` | **新建** — 6 段位阶梯策略 |
| `src/components/charts/RadarChart.tsx` | **新建** — ECharts 雷达图封装 |
| `src/pages/DimensionAnalysis.tsx` | 重写 — ECharts 雷达图 + 短板诊断 + 练习推荐 + 推分路线 |
| `src/utils/chartTags.ts` | **新建** — 7 种谱面特征标签 |
| `src/utils/weaknessAnalysis.ts` | **新建** — 个人短板诊断 |
| `src/utils/practiceRecommend.ts` | **新建** — 练习曲推荐算法 |
| `src/utils/routePlanner.ts` | **新建** — 推分路线规划 |
| `src/pages/PlayerInfo.tsx` | 修改 — 集成 PushSuggestions + StrategyPanel |
| `vite.config.ts` | 修改 — 新增 vitest test 配置 |
| `package.json` | 修改 — 安装 echarts/echarts-for-react/tslib/vitest/@types/node，新增 test/test:watch 脚本 |
