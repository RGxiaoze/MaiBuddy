# 开发日志 — 2026-06-10

## 完成事项

### 渲染错误修复与代码隐患清理
- [x] MainLayout `Outlet` 包裹 `ErrorBoundary`（防单页面崩溃全站白屏）
- [x] `statsService` 轮询等待 → Promise 队列（消活锁风险）
- [x] `SongDetail` useMemo 依赖对齐（`levelIndex`→`level`）
- [x] `SongList` pairMap 拆为独立 useMemo（翻页不再重建）
- [x] `SongDetail` useEffect 加 cancelled 清理标志
- [x] `PlayerInfo` getTotalNotes 预建 Map（O(1) 替代 O(n×m)）
- [x] `SongDetail` RateDiffCard 非空断言 → 显式 null 检查
- [x] 补全 `SortBy` 类型定义、移除 8 处未使用变量/导入
- [x] 修复 `tsc --noEmit` 实际不检查代码的配置问题（根 tsconfig.json `"files":[]`）

### B35/B15 预览模式 + 虚化提示
- [x] 本地 B15/B35: 折叠态显示前 5 条 + 渐变虚化遮罩 + "展开全部"按钮
- [x] DF 在线 B15/B35: 新增折叠状态，同预览模式
- [x] 推分建议: `SuggestionSection` 折叠时也渲染前 5 条表格
- [x] 桌面端第 5 行 `opacity-40`, 移动端 `opacity-50`，底部渐变遮罩
- [x] 空池显示"暂无建议"替代完全隐藏

### 推分建议双池算法修复
- [x] `pushSuggestions`: B35 新增渐进下界 `B35_STRETCH_LOWER`（同 B15 逻辑）
- [x] 扫描谱面从 `song.difficulties.dx` 扩展为 `[...dx, ...standard]`
- [x] `MAX_SUGGESTIONS=38` 全局上限 → `MAX_SUGGESTIONS_PER_POOL=30` 每池独立截断
- [x] 浏览器验证: 查分「想出无尽的小泽」(11424) → B35=30 / B15=30 双池正常

### 全服统计消失修复
- [x] 根因: `forceRefreshStats` → `resetStats()` 清内存，useEffect([songs.length]) 不重触发
- [x] 修复: `forceRefreshStats` 原地重建 `_chartStats`/`_levelAvgs`/`_fitDiffAvgs`，保持 `_loaded=true`

### 导入弹窗布局美化
- [x] 登录表单: 拆为用户名/密码独立行 + 各带标签 + 全宽按钮
- [x] 手动 Token: 同样垂直标签布局
- [x] 加载中按钮显示旋转动画
- [x] 弹窗打开时 body 滚动锁定 (`overflow:hidden`)

### 文档全面更新
- [x] `04-执行步骤.md`: Step 32 全勾选 + 新增 Step 50-55（v0.4.1 章节）
- [x] `progress-report.md`: 版本→v0.4.1, 测试数→105, 新增 v0.4.1 修复汇总
- [x] `CLAUDE.md`: 新增 v0.4.1 阶段 + 3 条已知陷阱
- [x] `开发日志/2026-06-10.md`: 新建

## 验证结果

```
npx tsc -p tsconfig.app.json --noEmit — 零错误
npx vitest run — 105/105 passed
npm run build — success (~870ms)
```

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/components/layout/MainLayout.tsx` | 修改 | Outlet ErrorBoundary 包裹 |
| `src/services/statsService.ts` | 修改 | Promise队列替代轮询 + forceRefreshStats原地重建 |
| `src/pages/SongDetail.tsx` | 修改 | useMemo依赖对齐 + useEffect清理 + 非空断言修复 |
| `src/pages/SongList.tsx` | 修改 | pairMap拆分 + 类型导入 + 弹窗布局美化 + body锁滚动 |
| `src/pages/PlayerInfo.tsx` | 修改 | totalNotesMap预建 + B35/B15预览 + DF预览 + 移除未使用导入 |
| `src/components/shared/PushSuggestions.tsx` | 修改 | SuggestionSection折叠预览 + 虚化 + 空池提示 |
| `src/store/songStore.ts` | 修改 | SortBy类型定义 |
| `src/config/algorithms.ts` | 修改 | MAX_SUGGESTIONS→MAX_SUGGESTIONS_PER_POOL |
| `src/utils/pushSuggestions.ts` | 修改 | B35下界渐进 + 标准谱面扫描 + 每池独立上限 |
| `src/utils/chartParser.ts` | 修改 | 移除未使用的SimaiFile变量 |
| `src/utils/configRecognition.ts` | 修改 | 未使用参数前缀_ |
| `src/utils/routePlanner.ts` | 修改 | 移除未使用的LEVEL_TIER导入 |
| `src/utils/__tests__/weaknessAnalysis.test.ts` | 修改 | 未使用参数前缀_ |
| `CLAUDE.md` | 修改 | 新增v0.4.1 + 已知陷阱 |
| `docs/04-执行步骤.md` | 修改 | Step 32 勾选 + Step 50-55 新增 |
| `docs/progress-report.md` | 修改 | 版本/测试数/v0.4.1汇总 |
| `开发日志/2026-06-10.md` | 新建 | 本文件 |
