# 执行步骤 — 舞萌DX 伴侣

## 阶段一：基础架构

### Step 1: 项目初始化
```bash
npm create vite@latest maimai-companion -- --template react-ts
cd maimai-companion
npm install react-router@latest zustand dexie dexie-react-hooks echarts echarts-for-react
npm install -D tailwindcss @tailwindcss/vite
```
- [x] 配置 vite.config.ts（@tailwindcss/vite + API 代理）
- [x] 配置 tsconfig.json（路径别名 @/）
- [x] 替换全局 CSS 为 TailwindCSS v4 方案
- [x] 定义主题 CSS 变量

### Step 2: 完整目录骨架
- [x] 创建 src/ 下所有子目录和占位文件
- [x] components/layout/ + components/shared/
- [x] pages/（4 个页面文件）
- [x] services/ / db/ / store/ / utils/ / types/ / data/

### Step 3: TypeScript 类型定义
- [x] Song / Chart / Difficulty 类型
- [x] Score / Grade / FcAp / DxScoreDetail 类型
- [x] API 响应类型

### Step 4: API 服务层
- [x] fetchMusicData() — 阶段一实现
- [x] fetchChartStats() — 函数签名
- [x] fetchPlayerScores() — 阶段二实现（POST 查询 Diving-Fish 玩家 B50）
- [x] 错误处理 + 超时 + 重试

### Step 5: IndexedDB 数据库
- [x] Dexie 数据库初始化（MaimaiDB）
- [x] scores 表定义
- [x] CRUD 方法（addScore / updateScore / deleteScore / getScoresBySong）
- [x] songCache 表（v2 迁移，24h TTL）

### Step 6: Zustand 状态管理
- [x] songStore（曲目列表 + 搜索 + 筛选 + IndexedDB 缓存优先加载）
- [x] scoreStore（成绩列表 + CRUD + IndexedDB 同步 + 本地数组变更）
- [x] playerStore（Diving-Fish 在线查分 + 本地 B50 计算）

### Step 7: 路由 + 布局框架
- [x] React Router 配置（/songs, /songs/:id, /player, /analysis）
- [x] MainLayout 组件（左侧边栏 + Outlet）
- [x] 导航高亮（NavLink）

### Step 8: 曲目检索页
- [x] 搜索栏（实时过滤 + useDeferredValue 防抖）
- [x] 高级搜索面板（定数/版本/作者筛选）
- [x] 曲目列表展示（预计算显示数据优化）
- [x] 加载/错误/空结果状态

### Step 9: 曲目详情页
- [x] 基础信息区（封面/曲名/作者/BPM）
- [x] 难度切换标签页
- [x] 谱面信息区（谱面作者/物量）
- [x] DX Rating 对照表（使用共享 computeRating）
- [x] 成绩模块（列表 + 录入按钮 + memo 优化）

### Step 10: 成绩录入表单
- [x] 模态框表单（所有字段）
- [x] 表单校验
- [x] IndexedDB 写入 + store 更新
- [x] 编辑/删除已有成绩

### Step 11: 共享组件
- [x] LoadingSpinner
- [x] ErrorMessage
- [x] EmptyState
- [x] DifficultyBadge
- [x] GradeBadge

---

## 阶段二：数据联网与统计

### Step 12: Rating 与 B50 算法
- [x] 修正 COEFFICIENT_TABLE（对齐 ScoreCoefficient.js 参考实现）
- [x] computeRating(levelValue, achievements) — 单曲 Rating
- [x] computeB50(scores, songMap) — B50 算法（Best35 旧版本 + Best15 新版本）
- [x] SongDetail 改用共享 computeRating

### Step 13: 曲目数据 IndexedDB 缓存
- [x] songCache 表（v2 数据库迁移）
- [x] songStore.fetchSongs() 缓存优先 + 后台刷新

### Step 14: Diving-Fish 玩家查分
- [x] fetchPlayerScores(username) — POST 查询 B50（接口：`/query/player`，无需认证）
- [x] adapter.toInternalDfScore() — ChartInfo 格式转换
- [x] 代理配置（Vite 已支持所有 HTTP 方法）
- [x] **修复**：请求体需加 `b50: true`，否则默认返回 B40 而非 B50
- [x] **错误处理**：不存在玩家返回 HTTP 200 + `{"message":"user not exists"}`，需检查 `data.charts`

### Step 15: 玩家信息页
- [x] Diving-Fish 在线查分（输入用户名 → B50 列表 + 总 Rating）
- [x] 本地 B50（基于 IndexedDB 成绩 + songStore 歌曲数据计算）
- [x] Tab 切换在线/本地
- [x] B50 列表展示（难度标签/曲名/达成率/评级/FC-FS/Rating）

### Step 16: 五维分析雏形
- [x] computeChartDimensions(chart, bpm) — 单谱面五维评分
- [x] computePlayerDimensions(scores, chartDimensions) — 玩家能力平方加权平均
- [x] 简易条形图展示（CSS bar，无需 ECharts）
- [x] 维度说明 + 代表谱面

## 阶段二 补充任务

### Step 17: Diving-Fish 全量成绩一键导入 ✅
- [x] 新增 `/api/maimaidxprober/player/records` 接口调用（GET，需 `Import-Token` header）
- [x] 在 PlayerInfo 页面增加 Import-Token 输入框和「导入全量成绩」按钮
- [x] 点击后拉取玩家**全部曲目 × 全部难度**的成绩（非仅 B50），批量存入 IndexedDB
- [x] 导入过程中显示进度提示，完成后显示新增/更新/跳过数量
- [x] 导入完成后自动刷新本地 B50 展示
- [x] **认证方式**：用户自行在 Diving-Fish 个人资料页生成 Import-Token，粘贴到 App
- [x] **自动获取 Token**：支持用户名+密码登录，自动调用 `/login` + `/player/import_token`（PUT）获取 Token
- [x] **Token 持久化**：Import-Token 自动存储到 localStorage，刷新页面后复用
- [x] **存储策略**：按 `[songId+levelIndex]` 去重，保留更高达成率（防止覆盖本地更好的成绩）
- [x] **错误处理**：Token 无效/玩家不存在/权限不足等提示
- [x] Vite 代理配置增加 `cookieDomainRewrite: 'localhost'`（支持登录 Cookie 转发）

### Step 18: 修复 B50 查询（仅返回 B40 的 Bug） ✅
- [x] `fetchPlayerScores` 请求体增加 `b50: true` 参数
- [x] 修复不存在玩家的错误处理（检查响应中 `charts` 字段）

### Step 19: 未来 LXNS 兼容预留
- [ ] `lxnsApi.ts` 接口桩代码保留，待后续实现
- [ ] 类型定义已就绪（`LxnsSong` 等）

### Step 20: v0.3.0 结构性调整 ✅
- [x] 多关键词 AND 搜索（空格分词，匹配 title/artist/noteDesigner/aliases）
- [x] 别名搜索（Yuri-YuzuChaN/maimaiDX 社区别名 API → 反向索引 → 子串匹配）
- [x] 定数预设按钮（13/13+/14/14+/15，.0-.5/.6-.9 分段）
- [x] 版本分国服筛选（日服框架预留）
- [x] 难度筛选（BASIC~Re:MASTER）
- [x] 侧边栏用户信息卡片（UserInfoCard）
- [x] 导入按钮迁移到曲目查询页（SongList 搜索栏同排）
- [x] PlayerInfo 移除 Import-Token 区块，保留双 Tab
- [x] 50条/页分页 + 页码导航
- [x] Sidebar/MobileBottomNav 标签更新（B50 一览）
- [x] 版本号 v0.2.0 → v0.3.0

### Step 21: DX 星数评分展示 ✅
- [x] 新增 `src/utils/dxStar.ts`（computeDxStar + renderStars）
- [x] SongDetail 成绩行展示 DX 星数（`DX 290/300 (96.7%)`）
- [x] PlayerInfo B50 行展示 DX 星数（`⭐ 96.7%`）+ FS 标记
- [x] `docs/05-算法详解.md` 新增「6. DX 星数评分」章节

### Step 22: 6 项展示修复 + 别名 API Bug 修复 ✅

**发现 Bug**：yuzuchan.moe 别名 API 返回 `{code: 0, content: AliasEntry[]}` 对象，代码期望裸数组 → `Array.isArray()` 永远返回 false → 别名索引永为 null → 所有别名搜索返回 0 结果

- [x] **Fix 1**: DX 分数率统一 `.toFixed(4)` 四位小数（dxStar.ts / SongDetail / PlayerInfo）
- [x] **Fix 2**: SongDetail 新增 ScoreHeader 表头组件，列宽与 ScoreRow 严格对齐
- [x] **Fix 3**: ScoreRow DX 分数列展示星数图标（`⭐⭐ DX 3000/3315 (90.4977%)`）
- [x] **Fix 4**: ScoreForm 切换难度自动填充 dxMax = totalNotes×3，自动展开折叠区
- [x] **Fix 5a**: App.tsx `useEffect` 调用 `loadAliasData()` 初始化别名
- [x] **Fix 5b**: aliases.ts 构建正向索引 `getAliasesForSong()`（仅社区别名，不含官方曲名）
- [x] **Fix 5c**: SongDetail 异步展示社区别名标签
- [x] **Fix 6**: PlayerInfo B50Row FC/FS + DX 星数列始终渲染占位，保证列对齐
- [x] **别名 API 修复**: `aliases.ts` 兼容 `{code, content}` 和裸数组两种响应格式
- [x] **浏览器验证**: agent-browser + Edge 逐项验证全部 8 项检查点通过
- [x] **构建验证**: TypeScript 零错误，Vite 构建通过 (249ms)

---

## 阶段三：推分建议（已完成）

### Step 23: 推分建议核心算法 ✅
- [x] `src/utils/rating.ts` — `computePushSuggestions(scores, songMap, currentB50)` 推分候选池算法
- [x] `src/utils/rating.ts` — `classifyDifficulty(achievements)` 难度分类（easy/medium/hard）
- [x] `src/utils/rating.ts` — `makeSuggestion()` 目标达成率固定 100.5%
- [x] `src/utils/rating.ts` — 增益计算公式：floor35/floor15 地板替换逻辑
- [x] B50 归池逻辑：`song.isNew`（来自 `basic_info.is_new`）决定 b35/b15

### Step 24: 推分建议 UI ✅
- [x] `src/components/shared/PushSuggestions.tsx` — 推分建议面板组件
- [x] 成绩池标签 `POOL_LABELS`（旧版本 Best 35 / 新版本 Best 15）
- [x] 表格列：曲目/定数/当前/目标/增益/难度/成绩池
- [x] 前 5 条默认展示 + 展开/收起全部
- [x] `PushRow` React.memo 优化

### Step 25: 推分建议 Bug 修复 (A1/A2/A3) ✅
- [x] A1：表头 "替换池" → "成绩池"
- [x] A2：目标列显示 `targetAchievements`（达成率 %）而非 `ratingGain`
- [x] A3：增益列显示 `ratingGain`（总 Rating 增量，如 +14）

### Step 26: 五维分析页面整合 ✅
- [x] `src/pages/DimensionAnalysis.tsx` — 整合雷达图/短板/推分路线
- [x] `src/utils/dimensions.ts` — `computePlayerDimensions()` 平方加权平均
- [x] `src/utils/chartTags.ts` — `classifyChart()` 7 种谱面标签分类
- [x] `src/utils/weaknessAnalysis.ts` — `analyzeWeakness()` 短板诊断（≥3 条/标签）
- [x] `src/utils/practiceRecommend.ts` — `recommendPracticeSongs()` 递进练习推荐
- [x] `src/utils/routePlanner.ts` — `generatePushRoute()` 4 阶段推分路线

### Step 27: 雷达图组件 ✅
- [x] `src/components/charts/RadarChart.tsx` — ECharts 雷达图（echarts-for-react ESM 入口）
- [x] 修复 CJS→ESM 导入 Bug（`echarts-for-react/lib/core` → `echarts-for-react`）
- [x] 修复 DimensionAnalysis TDZ Bug（变量名 `songs` 遮蔽）

### Step 28: P0-P1 验收 Bug 修复 ✅
- [x] Bug 1: 五维 0.99 → 三次加权 `Σ(s³×f)/Σ(s²)` 修复（`dimensions.ts`）
- [x] Bug 2: B15/B35 分类错误 → `song.isNew` 约束池分配（`rating.ts`）
- [x] Bug 3: "15400+" 解析失败 → 拆为 15400-16200 / 16200+ 两档（`build-kb.js` / `knowledge-base.md`）
- [x] Bug 4: 推分建议过于激进 → 天花板分析 + `realisticTargetAch()` + `MAX_LEVEL_GAP=1.5`（`rating.ts`）
- [x] Bug 5: 语言风格网游化 → Ranker/万六/极牌 等社区用语（`strategy.ts` / `knowledge-base.md`）
- [x] Bug 6: 在线查询切页清空 → `useState(dfPlayerName)` + 移除重复声明（`PlayerInfo.tsx`）
- [x] Bug 7: SongDetail 直接 URL 白屏 → Hooks 移到 early return 之前（`SongDetail.tsx` — React Hooks 顺序违规）
- 涉及：`dimensions.ts` / `rating.ts` / `strategy.ts` / `PlayerInfo.tsx` / `SongDetail.tsx` / `build-kb.js` / `knowledge-base.md`

### Step 29: P2 推分算法 & 策略分段优化 ✅
- [x] `computeTheoreticalMaxRating(songs)` — 理论最高 Rating（`rating.ts`）
- [x] `computeLevelMode()` — B35 定数众数，替代 90分位天花板（`rating.ts`）
- [x] 众数区间过滤 `mode+0.1~0.5` 替代 `ceiling+1.5`（`rating.ts`）
- [x] SSS+ 率加权排序 `gain×(1+rate×2)` 优先水分曲（`rating.ts`）
- [x] PushSuggestions 显示 "当前 / 理论最高"（`PushSuggestions.tsx`）
- [x] 策略分段 6→9 档：新增 冲击万六/万六五/冲击理论/理论Rating（`strategy.ts`）
- [x] 理论档 AP 查漏 — 遍历 14-14+ 未 AP 谱面（`strategy.ts`）
- [x] PlayerInfo 计算并传递 theoreticalMax（`PlayerInfo.tsx`）
- [x] 知识库文案更新 15000-16000/16000-16200/16200-16400/16400+（`knowledge-base.md`）
- [x] 测试 24/24 通过，构建零错误
- 涉及：`rating.ts` / `rating.test.ts` / `strategy.ts` / `PlayerInfo.tsx` / `PushSuggestions.tsx` / `knowledge-base.md`

### Step 30a: 阶段四算法模块重构 ✅

**批次 A：配置提取**
- [x] 新建 `src/config/algorithms.ts` — 60+ 业务常量集中管理（B50/Rating/推分段位/定数分段/谱面分类/五维归一化/短板分析），全部加中文注释
- [x] 更新 7 个算法文件的魔法数字引用为配置导入（rating/b50/pushSuggestions/difficultyTier/chartTags/dimensions/weaknessAnalysis/practiceRecommend/routePlanner/strategy）

**批次 B：文件拆分 + JSDoc**
- [x] `rating.ts` 449 行拆分为 4 个独立文件：`rating.ts`（仅 computeRating，42 行）/ `b50.ts` / `pushSuggestions.ts` / `difficultyTier.ts`
- [x] 所有导出函数添加完整 JSDoc（@param/@returns/算法步骤），类型导出添加 @description
- [x] 更新 7 个消费文件的 import 路径（PlayerInfo/DimensionAnalysis/PushSuggestions/RadarChart/routePlanner/strategy/playerStore）

**批次 C：测试 + 文档 + Tooltip**
- [x] 新建 5 个测试文件：`dxStar.test.ts`（9）/ `difficultyTier.test.ts`（14）/ `chartTags.test.ts`（7）/ `b50.test.ts`（9）/ 更新 `rating.test.ts`（26）— 共 65 用例
- [x] 新建 `docs/algorithms.md` — 玩家友好算法说明（DX Rating/B50/推分建议/路线/谱面标签/五维/DX星/全服分布），不含代码
- [x] 新建 `src/components/shared/InfoTooltip.tsx` — hover 弹出解释组件（lucide Info 图标 + CSS popover）

**验证**：`tsc --noEmit` 零错误 / `vitest run` 65/65 通过 / `npm run build` 677ms

涉及：`config/algorithms.ts` / `b50.ts` / `pushSuggestions.ts` / `difficultyTier.ts` / `InfoTooltip.tsx` / `docs/algorithms.md` / 5 个测试文件 / 13 个修改文件

### Step 30b: 全服达成分布卡片优化 ✅

- [x] 显示差值而非绝对值：SSS率/SSS+率/AP率与同级均值对比，红（低于均值=偏难）/绿（高于均值=水分）/灰（差距小）
- [x] 混合统计：官标同等级（如 14+）为主 + 同定数（如 14.3）兜底
- [x] 灰化边界：基于 5,286 谱面真实数据分布分析，按率分别设定（SSS±2.0pp / SSS+±2.0pp / AP±0.3pp / fit_diff±0.05）
- [x] fit_diff 差值行：显示社区拟合定数与官标的偏差，正数红色/负数绿色（与率差值颜色逻辑相反）
- [x] 评级标签配色：SSS/SSS+ 彩虹色、AP 橙色（对齐全局评级色彩体系）
- [x] 标准差 4 级分类标注（<3.6 正常 / 3.6-4.2 偏高 / 4.2-4.8 高 / ≥4.8 极高）
- [x] 修复 `buildChartStats()` diff_data key 类型 Bug（`Math.floor(fit_diff)` → `entry.diff`，`diff` 为 `string` 类型）

涉及：`statsService.ts` / `divingFishApi.ts` / `SongDetail.tsx`

### Step 31: 项目结构审查与清理 ✅

- [x] 删除 6 张调试截图 + `参考项目/` 目录（12MB+）
- [x] 卸载未使用依赖：`tslib`（0 处 import）、`playwright`（无测试文件）
- [x] 测试文件位置统一：`rating.test.ts` → `__tests__/rating.test.ts`
- [x] 组件位置规范：`ScoreForm.tsx` → `shared/ScoreForm.tsx`
- [x] `.gitignore` 新增 `*.png`、`public/data/kb.json`
- [x] 从 Git 跟踪移除 `public/data/kb.json`（prebuild 自动生成）
- [x] 文档全面更新：package.json 0.2.0→0.4.0、README 重写、CLAUDE.md 同步、04-执行步骤追加重构/清理步骤
- [x] 开发日志：06-04 追加重构/卡片/统计分析、06-05 新建

**验证**：`tsc --noEmit` 零错误 / `vitest run` 65/65 通过

---

## 阶段四：五维算法优化

> **背景**：P0-P1 验收后，五维雷达图已修复为有区分度的正五边形（`Σ(s³×f)/Σ(s²)` 三次加权）。但当前算法存在三个根因导致无法区分高端玩家（普通万六/万六五/理论Rating/舞神）。

### 根因分析

| 问题 | 说明 |
|------|------|
| 体力公式反直觉 | `stamina = total²/BPM×60` — 同物量下低 BPM 反而体力分更高 |
| 爆发公式忽略密度 | `burst = BPM²/40` — 与物量完全无关，100 note 和 2000 note 同分 |
| 聚合过于粗糙 | 立方加权 + 6 档达成率因子，万六和舞神雷达图几乎重叠 |

### P0：数值统计指标（P3 优先）

在不改五维算法本身的前提下，增加可量化区分指标：

- [ ] **AP 计数**：`fcType='ap'|'app'` 数量。舞神 >50，普通万六 <10
- [ ] **SSS+ 计数**：`achievements >= 100.5` 的谱面数
- [ ] **Rating 进度**：`totalRating / theoreticalMaxRating × 100%`
- [ ] **定数区间分布**：按 13.x / 14.x / 15.x 分组统计平均达成率
- 涉及：`src/utils/dimensions.ts` — 新增 `computeAchievementMetrics(scores, songMap)` 导出函数
- 涉及：`src/pages/DimensionAnalysis.tsx` — 在雷达图下方渲染指标卡片

### P1：按定数区间分组五维（P3）

将单一的五维评分拆分为按定数区间分组的多维视图：

- [ ] `computeDimensionsByLevelRange(scores, chartDimensions, ranges)` — 返回 `Map<string, DimensionScores>`
- [ ] 区间划分：13.0-13.9 / 14.0-14.5 / 14.6-14.9 / 15.0+
- [ ] 雷达图变为 4 组并排或堆叠展示，展现舒适区 vs 极限区差异
- [ ] 万六用户：13-14 区间强，15+ 区间弱
- [ ] 舞神用户：全部区间接近满分
- 涉及：`src/utils/dimensions.ts` / `src/pages/DimensionAnalysis.tsx`

### P2：修复体力/爆发公式 + 重新校准（P4）

- [ ] **体力公式**：`staminaRaw = total × estimatedLength × (1 + BPM / 200)` — 引入 BPM 乘数
- [ ] **爆发公式**：`burstRaw = BPM × total / estimatedLength × (tapPct + breakPct)` — 引入物量密度 + 击打类比重
- [ ] 运行 `calibrateNormMax()` 获取新的 NORM_MAX 值并填入模块常量
- [ ] 浏览器 Console 验证新旧公式对典型谱面的区分度
- 涉及：`src/utils/dimensions.ts`

### P2：舒适区/极限区双环雷达图（P4）

基于 P1 的分组数据，在 ECharts 雷达图上同时展示两个系列：

- [ ] **内环（舒适区）**：基于 B35 众数以下的谱面分组计算
- [ ] **外环（极限区）**：基于 B35 众数以上的谱面分组计算
- [ ] 双环间距 = 成长空间。舞神间距极小，普通万六间距大
- 涉及：`src/components/charts/RadarChart.tsx` — 扩展支持多系列
- 涉及：`src/pages/DimensionAnalysis.tsx` — 构建双环数据

### P0：短板分析基准化（新增 — 待用户确认后启动）

> **背景**：当前 `analyzeWeakness()` 存在四个根因——无基准对比（只看自身不看同段位）、未控制定数（不同难度混在一起）、标签粗糙（7种基于全局物量，无法识别扫键/圈）、无统计置信（2-3首就下结论）。

**方向1+2+4 合并**（P0，不改标签体系）：

- [ ] 引入 chart_stats 基准：`residual = actual − avg`，按标签聚合残差
- [ ] 按定数区间分层（13.0-13.9 / 14.0-14.5 / 14.6-14.9 / 15.0+），层内分别计算
- [ ] 残差显著为负（< −1.5%，样本 ≥ 5）的标签才标记为短板
- [ ] 无显著短板时显示"各项发展均衡"
- [ ] 显示样本量（N）、残差标准差（σ）、拖分谱面 Top 3
- 涉及：`src/utils/weaknessAnalysis.ts` — 重写 `analyzeWeakness()`
- 涉及：`src/pages/DimensionAnalysis.tsx` — 短板分析 UI 重写

### P1：谱面配置解析调研（新增 — 独立子项目，待用户确认后启动）

> **背景**：当前 `classifyChart()` 基于全局物量统计（`tapPct > 0.60 && bpm > 160 → 交互`），无法反映谱面配置特征（扫键、圈、特殊SLIDE路径）。社区公认的典型谱面特征在物量统计中完全不可见。

- [ ] 研究社区谱面文件格式（Simai/.ma2 解析库），评估逐音符解析的可行性
- [ ] 如可行，设计解析器架构：读入谱面配置 → 产出音符序列 ast → 提取特征向量
- [ ] 特征包含：扫键（跨lane快速交替TAP）、圈（SLIDE路径圆形度）、纵连（同lane密集TAP/HOLD）、交互（多lane交替）、体力、技巧
- [ ] 用社区公认的典型谱面验证标签准确性
- 涉及：新建 `src/utils/chartParser/` 子目录

### P2：标签体系替换（新增 — 依赖 P1 完成）

- [ ] 替换 `classifyChart()`，接入谱面配置解析器
- [ ] 更新 `weaknessAnalysis.ts` / `practiceRecommend.ts` 标签引用
- [ ] 回归验证：确保现有谱面分类结果不出现严重退化

---

## 阶段五：网站完善（新增页面/组件 + Bug 修复）

### Step 32: 新增页面与导航入口

**A. 使用指南页面** (`/guide`)
- [ ] 新建 `src/pages/Guide.tsx`（纯文字 + 图标，无截图）
- [ ] 路由注册 `lazy(() => import('./pages/Guide'))`
- [ ] 侧边栏添加「使用指南」导航项

**B. 更新记录页面** (`/changelog`)
- [ ] 新建 `src/pages/Changelog.tsx`（从 Git commit 自动生成，按约定格式解析）
- [ ] 路由注册
- [ ] 侧边栏添加「更新记录」导航项

**C. 页脚组件**
- [ ] 赞助链接（URL 暂留占位）
- [ ] GitHub 链接（URL 暂留占位）

**D. 侧边栏「关于」折叠区**
- [ ] 参考数据来源与致谢
- [ ] 开发者文档入口（本地 `/docs` 路由预览 Markdown + GitHub 外链按钮）
- [ ] GitHub 图标链接

**E. 本地文档路由** (`/docs`)
- [ ] 新建 `src/pages/Docs.tsx` — 渲染 `docs/` 目录下的 Markdown 文件
- [ ] 路由注册
- [ ] GitHub 外链按钮（跳转到仓库 docs/ 目录）

### Step 33: Bug 修复 — 同定数对比桶查询键错误 ✅

- [x] **问题**：`SongDetail.tsx` 中 `getFitDiffAvg(currentDiff.levelValue)` 使用官方定数查询 fit_diff 桶，但 `_fitDiffAvgs` 按社区拟合定数分组，导致查询到错误桶的平均值
- [x] **修复**：`getFitDiffAvg()` 参数从 `currentDiff.levelValue` 改为 `chartStats.fitDiff`（使用该谱面的实际拟合定数查找对应桶）
- [x] **新增功能**：三行对比体系
  - 第一行 `vs 官标等级` — 与同难度等级（如 14+）全服平均对比（已有）
  - 第二行 `vs 官标定数` — 与同官方定数（如 14.7）全服平均对比（新增 `getOfficialLevelAvg`，交叉关联 music_data）
  - 第三行 `拟合后 vs 官标定数` — 与同拟合定数（如 14.9）全服平均对比（修正后）
- [x] `statsService.ts` 新增 `buildOfficialLevelAvgs()` / `getOfficialLevelAvg()`，`loadStats` 接受 `officialLevelMap` 可选参数
- [x] `loadStats` 支持二次调用补建 `_officialLevelAvgs`（解决其他组件先加载无 map 的竞态问题）
- [x] 附带修复：重新安装 `tslib`（`echarts-for-react` 构建依赖，阶段三清理时误删）

### Step 34: 收藏品查询（P3，待有 API）
- [ ] 优先级 P3，等待可用收藏品 API 后再启动

### 日后计划

1. **DX 分数纳入五维评分**：将 DX 分数率作为判定精度补充指标
2. **保护套算法研究**：深入研究 EX-TAP/EX-HOLD 对谱面难度评估的影响
3. **DX 星数颜色化**（可选）：未来可为不同星数配不同颜色提升视觉辨识度
